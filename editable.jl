#minimum bribes

initialArray = readline();
testCase = readline();

foo(initialArray[1], testCase);

function foo(element,test)
	distance = 0;
	string(element);
	
	println(typeof(element));
	
	return distance;
end



